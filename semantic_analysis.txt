semantic analysis
- auto string not allowed as struct field
- auto string only allowed as last function param or return
- custom data type must be is the list of structs or enums
- custom enum or struct not referenced in any function or any other custom type
- duplicate interface IDs
- duplicate function IDs (in same interface)
- duplicate enum field IDs (in same enum)
- duplicate interface names
- duplicate function names (in same interface)
- duplicate enum field names (in same enum)
- duplicate struct field names (in same struct)